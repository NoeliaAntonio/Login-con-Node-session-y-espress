doctype html
head
  meta(charset='utf-8')
  meta(name='viewport' content='width=device-width,minimum-scale=1')
  title Noelia
  // La biblioteca Form Awesome se utiliza para agregar íconos a nuestro formulario
  link(rel='stylesheet' href='https://use.fontawesome.com/releases/v5.7.1/css/all.css')
  // Incluir el archivo de hoja de estilo
  link(href='/style.css' rel='stylesheet' type='text/css')
  //p Para acceder a la sesión accedemos a req.session en donde req también es llamada petición o request. Así de simple; todo esto usando JavaScript del lado del cliente.
.login
  h1
    | Login  | 
    a(href='https://www.configuroweb.com/46-aplicaciones-gratuitas-en-php-python-y-javascript/#Aplicaciones-gratuitas-en-PHP,-Python-y-Javascript') Noelia
  form(action='/auth' method='post')
    label(for='username')
      // icono de font awesome
      i.fas.fa-user
    input#username(type='text' name='username' placeholder='Usuario' required='')
    label(for='password')
      i.fas.fa-lock
    input#password(type='password' name='password' placeholder='Contraseña' required='')
    input(type='submit' value='Ingresar')

